spring.application.name=oneStar3gram

spring.datasource.url=${DATABASE_URL}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DATABASE_USER}
spring.datasource.password=${DATABASE_PASSWORD}
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

## keycloak
spring.security.oauth2.client.provider.onestar3gram-keycloak.issuer-uri=https://proxy-onestar3gram:8080/realms/OneStar3gram

spring.security.oauth2.client.registration.keycloak.provider=onestar3gram-keycloak
spring.security.oauth2.client.registration.keycloak.client-name=onestar3gram-backend
spring.security.oauth2.client.registration.keycloak.client-id=onestar3gram-backend
spring.security.oauth2.client.registration.keycloak.scope=openid,offline_access,profile
spring.security.oauth2.client.registration.keycloak.client-secret=${KEYCLOAK_SECRET}
spring.security.oauth2.client.registration.keycloak.authorization-grant-type=authorization_code

server.forward-headers-strategy=framework

springdoc.swagger-ui.csrf.enabled=true

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB